# Voice Cloning TTS System - TTS 2.0 Requirements
# ===============================================

# Core TTS Libraries - TTS 2.0
coqui-tts>=0.27.0   # Advanced TTS with voice cloning (XTTS v2)
torch>=2.1.0        # PyTorch for ML models
torchaudio>=2.1.0   # Audio processing for PyTorch

# Audio Processing
numpy>=1.26.0       # Numerical computing
soundfile>=0.12.0   # Audio file I/O
librosa>=0.11.0     # Audio analysis and processing
pydub>=0.25.1       # Audio manipulation (optional)

# Queue Consumer Service
pika>=1.3.0         # RabbitMQ client

# HTTP and Utilities
requests>=2.31.0    # HTTP client
tqdm>=4.64.0        # Progress bars

# Text Processing
regex>=2021.8.0     # Advanced regex for text cleaning

# System Utilities
psutil>=5.8.0       # System monitoring

# Database Integration
psycopg2-binary>=2.9.9  # PostgreSQL adapter

# HTTP Client for Storage Service
requests>=2.31.0  # HTTP client for local storage service

# Note: coqpit-config is automatically installed with coqui-tts
# Note: pyaudio and pygame are optional and may cause build issues
# Note: realtimetts removed as we only use Coqui TTS 