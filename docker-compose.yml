services:
  whatsapp-video-generator:
    build: .
    container_name: whatsapp-video-generator
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./output:/app/video_generator/output
      - ./background_videos:/app/background_videos
    ports:
      - "3001:3001"
    command: ["--prompt", "Uma conversa engraçada sobre tecnologia"] 